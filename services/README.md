# Services

This directory contains standalone local services used by BranchWhisper.

Current services:

- `tts/`: trained CosyVoice TTS API server.

ASR and LLM services are usually external commands managed by the backend service controller.
